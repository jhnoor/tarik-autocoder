apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tarik.fullname" . }}-test-connection"
  labels:
    {{- include "tarik.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tarik.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
